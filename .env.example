NEXT_PUBLIC_GTM=GTM-XXXXXXX           # optional: Google Tag Manager ID (public)
NEXT_PUBLIC_APP_URL=https://your-site.example.com  # optional: public URL used in frontend links

# Email (used by server-side APIs only)
EMAIL_ADDRESS=your email

# Gmail authentication
# Option A (recommended for simplicity): Gmail App Password (requires 2FA)
GMAIL_PASSKEY=16digit GMAIL_PASSKEY
